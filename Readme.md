To start this application in dev mode, first clone this repo locally then run:

```bash
docker compose -f docker-compose.dev.yml up --build
```

You can now modify the files with hot reloading enabled thanks to nodemon.